import { DashboardOfflineClient } from "@/components/features/dashboard-offline-client";

export const metadata = { title: "Dashboard — lostbae" };

export default function DashboardPage() {
  return <DashboardOfflineClient />;
}
