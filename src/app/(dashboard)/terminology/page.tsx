import { TerminologyOfflinePage } from "@/components/features/terminology-offline-page";

export const metadata = { title: "Terminology — lostbae" };

export default function TerminologyPage() {
  return <TerminologyOfflinePage />;
}
